# CLAUDE.md

**Read [`AGENTS.md`](./AGENTS.md) now.** It is the canonical brief for this
repo and the guidance is identical for every assistant — this file exists only
because Claude Code auto-loads `CLAUDE.md`, and keeping the content in one
place is what stops the two drifting apart.

One thing repeated here, because it is the expensive mistake to make before
reading anything:

> **The site is deliberately closed to the public** pending a lawyer's review.
> `FORCE_CLOSED = true` in `lib/site/access.ts` is intentional — it is not a
> bug, and it must not be flipped without being asked. `AGENTS.md` §2 explains
> what the closure covers and how to reopen it when the time comes.

Everything else — architecture, open defects, branch discipline, and the
gotchas worth knowing before you spend time on them — is in `AGENTS.md`.
